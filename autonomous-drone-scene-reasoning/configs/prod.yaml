# Prod profile: conservative tokens, minimal logging.
# Load with CONFIG_PROFILE=prod

cosmos:
  timing: false
  json_repair: false
  image:
    max_new_tokens: 1024
  video:
    max_new_tokens: 1024
  max_new_tokens_normalize: 1024
  max_new_tokens_explanation: 1024
  max_new_tokens_repair: 1024

agent:
  eval_log_every: 25
