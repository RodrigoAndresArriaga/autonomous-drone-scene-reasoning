# Dev profile: timing, larger tokens, verbose logging.
# Load with CONFIG_PROFILE=dev

cosmos:
  timing: true
  json_repair: true
  image:
    max_new_tokens: 1024
  video:
    max_new_tokens: 1024
  max_new_tokens_normalize: 1024
  max_new_tokens_explanation: 320
  max_new_tokens_repair: 1024

agent:
  eval_log_every: 5
